apiVersion: v1
kind: Service
metadata:
  name: keycloak-external-service-clusterip
  labels:        
    partof: keycloak
    created-by: Ed-Mar
spec:
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8080
      protocol: TCP
    - name: https
      port: 8443
      protocol: TCP
  type: ClusterIP